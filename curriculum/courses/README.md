# Courses

Create one directory per course using a stable, filesystem-safe course ID.
Start from the contracts in `schemas/`; do not add real curriculum to examples
or tests.

`dsl204/` is the first real SSOT proof. Its Week 5 record represents the two
approved administrative lesson plans; the `.docx` files remain reference
documents, not sources used by the generator.
