# Templates

The two minimal Markdown templates have identical content because templates
define structure while theme tokens define appearance. Institution profiles
contain repository-relative template selections to demonstrate ownership of
presentation configuration.

The frozen v2 session renderers produce deterministic Markdown directly and do
not accept a session-template argument, so neither template is applied by the
current canonical renderer.
