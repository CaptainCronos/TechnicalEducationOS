# Institutional resources

Owns registered academic calendars, meeting patterns, holidays, exam windows,
instructional-time rules, and school policies.

Inputs: approved institution records.  
Outputs: versioned, addressable constraints consumed by course blueprints.

Institution branding and renderer configuration belong in `institutions/` or
`templates/`; course schedules belong in `curriculum/blueprints/`.
