# Performance baselines

These optional tests measure representative operations and record timings in
JUnit user properties. Guards are regression alarms, not optimization targets.
Run them on an otherwise idle machine with `pytest -m performance -s`.
